import { cn } from "@/lib/utils";

const Error = ({className, title}:{
    className?:string
    title: string
}) => {
    return (
        <div className={cn('', className)}>
            
        </div>
    );
}
 
export default Error;