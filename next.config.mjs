/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['utfs.io', 'github.com','avatars.githubusercontent.com', 'uploadthing.com'],
    },
};

export default nextConfig;
